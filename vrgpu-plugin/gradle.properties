# The released client the plugin is built against, and the lwjgl version that
# client ships. These must match the client that will load the jar. Single source
# of truth: the build and the release workflow both read them from here.
runeliteVersion=1.12.33
lwjglVersion=3.3.2
